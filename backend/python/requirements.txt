# Core dependencies for audio transcription and AI analysis
# Install these in order for best compatibility

# Step 1: Install compatible numpy first (Whisper requires < 2.0)
numpy==1.26.4

# Step 2: Install PyTorch (CPU-only version to save space)
torch==2.1.2
torchaudio==2.1.2

# Step 3: Install main packages
openai-whisper==20231117  # Audio transcription
requests==2.31.0  # HTTP requests for Ollama/APIs

# Step 4: Text extraction from documents
pdfplumber==0.11.0  # PDF text extraction
ebooklib==0.18  # EPUB text extraction
beautifulsoup4==4.12.3  # HTML parsing for EPUB

# Step 5: AI provider SDKs (for cloud AI services)
openai>=1.0.0  # For GPT models
anthropic>=0.18.0  # For Claude API
